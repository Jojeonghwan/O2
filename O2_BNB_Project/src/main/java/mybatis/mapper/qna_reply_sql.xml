<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="review">
	<!-- 리뷰 등록 -->
	<insert id="qna_reply_insert" parameterType="o2.data.qna_reply_dto">
		INSERT INTO o2_qna_review 
		VALUES (
				seq1.nextval,
				#{q_num},
				#{id},
				#{pw},
				#{subject},
				#{content},
				sysdate
				)
	</insert>
	
	<!-- qna당  -->
	<select id="reply_totalCount" parameterType="HashMap" resultType="int">
		select  count(*)
		from o2_user_pay 
		where h_num = #{h_num} 
	</select> 
</mapper>